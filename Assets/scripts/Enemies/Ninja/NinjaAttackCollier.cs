using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NinjaAttackCollier : MonoBehaviour
{

  /*  [SerializeField]
    string colliderName;
    Ninja ninja;
    void Start()
    {
        ninja = transform.parent.GetComponent<Ninja>();
    }

    void Update()
    {
        
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if((collision.tag == "Player") && (!ninja.isAttacking))
        {
            ninja.SetAttackType(colliderName);
        }
        if(((collision.tag == "Player") || (collision.tag == "enemy")) && (colliderName == "hit"))
        {
            ninja.HitObject(collision.gameObject);
        }
    }*/
}
