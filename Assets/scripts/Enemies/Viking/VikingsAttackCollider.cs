using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VikingsAttackCollider : MonoBehaviour
{

  /*  [SerializeField]
    string attackType;
    Viking viking;
    void Start()
    {
        viking = transform.parent.GetComponent<Viking>();
    }

    void Update()
    {
        
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if(collision.tag == "Player")
        {
            if(attackType == "hitThePlayer")
            {
                viking.HitPlayer(collision.gameObject);
            }
            else
            {

                viking.SetAttackType(attackType);
            }
        }
        if((collision.tag == "enemy") && (attackType == "hitThePlayer"))
        {
            viking.HitEnemy(collision.gameObject);
        }*/
    
}
